<UserControl x:Class="Jibbr.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:cal="clr-namespace:Caliburn.Micro;assembly=Caliburn.Micro" 
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="25*" />
            <ColumnDefinition Width="50*" />
            <ColumnDefinition Width="25*" />
        </Grid.ColumnDefinitions>
        
        <ListView Grid.Column="0" SelectedItem="{Binding SelectedChatSession}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Target}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        <!-- Current chat window -->
        <ContentControl x:Name="SelectedChatSession" Grid.Column="1" />
        
        <!-- List of accounts currently in use -->
        <StackPanel Grid.Column="2">
            <ListView ItemsSource="{Binding Accounts}">
                <ListView.ItemTemplate>
                    <DataTemplate >
                        <Expander Header="{Binding Path=AccountJid}" FontWeight="Bold">
                            <ListView ItemsSource="{Binding Path=Friends}">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}" />
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Expander>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</UserControl>
